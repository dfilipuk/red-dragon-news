<html>
    <body>
    <h3>{{ 'mail.head.part.1'|trans }} {{ type }} {{ 'mail.head.part.2'|trans }}</h3>
        <ul>
            {% for one_post in posts %}
                    <li><a href="http://{{ host }}{{ path('news-page', {'id': one_post.id}) }}">{{ one_post.title }}</a></li>
            {% endfor %}
        </ul>
    </body>
</html>